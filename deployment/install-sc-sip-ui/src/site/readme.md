This archive contains installation docs for project sc-sip-ui, version ${versionWithoutSnapshotSuffix}.

Information for the release are the following :
   Release Doc    Url     : ${RELEASE_URL}   = ${artifactoryDocUrl}
   Release Binary Url     : ${RELEASE_URL}   = ${artifactoryBinUrl}
   Release Binary Archive : ${RELEASE_TARGZ} = ${artifactoryBinTargz}
   Release Binary name    : ${RELEASE_NAME}  = sc-sip-ui-release-${versionWithoutSnapshotSuffix}


To ensure a release installation, please refer to document installation_guide-sc-sip-ui-${versionWithoutSnapshotSuffix}.pdf.
   Installation is feasible only if a previous version of this application have been previously installed on the servers.

To ensure a release full installation, please refer to document full_installation_guide-sc-sip-ui-${versionWithoutSnapshotSuffix}.pdf.
   This procedure is application when no previous installation have been acheived on the server.
